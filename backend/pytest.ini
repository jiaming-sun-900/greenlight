[pytest]
# Put backend/ on sys.path so the tests can `from main import ...` when pytest is
# run as `pytest` rather than `python -m pytest`, which inserts the working
# directory itself.
pythonpath = .
testpaths = tests
